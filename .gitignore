# macOS
.DS_Store

# редакторы
.vscode/
.idea/
*.swp

# Оркестр теперь живёт ВНУТРИ кита и едет вместе с клоном:
#   /.claude/  — скиллы и агенты оркестра (start, feature, scout, builder, …)
#   /brain/    — стартовая память проекта (сид)
# поэтому они НЕ игнорируются. Игнорируем только то, что генерится под каждый
# запуск и у каждого своё:
/PROJECT.md
/CHANGELOG-DESIGN.md

# архивы сборок
*.zip
