# Changelog

Все заметные изменения Design Kit фиксируются здесь.

Формат — [Keep a Changelog](https://keepachangelog.com/ru/1.1.0/),
версии — по [Semantic Versioning](https://semver.org/lang/ru/): `MAJOR.MINOR.PATCH`.

Что версионируем этим файлом: **содержимое кита** — правила (`ux-lab/rules/`),
токены (`ux-lab/foundation/`), плейбук, шаблоны и база лучших практик.
Версию держим в файле [`VERSION`](VERSION).

> Не путать с **версионированием дизайна**: историю самих макетов ведёт оркестр
> в `CHANGELOG-DESIGN.md` внутри проекта (генерируется агентом Chronicler). См.
> [`RUNNING.md`](RUNNING.md).

## Когда какую цифру двигать

- **MAJOR** — несовместимое изменение процесса: переименование/удаление разделов
  спеки, ломающее старые спеки; смена структуры папок; замена Definition of Done.
- **MINOR** — обратно совместимое пополнение: новый паттерн в `best-practices/`,
  новое правило в `rules/`, новая фаза плейбука, новые токены.
- **PATCH** — правки без смены смысла: опечатки, уточнения формулировок, битые ссылки.

Правило: любое изменение в `ux-lab/` или `specs/` → строка в этот файл +
поднятая версия в `VERSION` в том же коммите.

## [Unreleased]

_Ничего пока._

## [1.9.0] — 2026-08-11

### Added
- Каталог шаблонов `ux-lab/foundation/templates/` (верхний слой атомарного дизайна:
  экраны из компонентов): README-протокол, `_TEMPLATE.md`, `INDEX.md`.
- Первый шаблон `create.md` — экраны создания сущности (Создание заявки, Рац.
  предложение/калькулятор) из файла Life-04_Templates, сверено с Figma.

## [1.8.0] — 2026-08-11

### Added
- Каталог компонентов Kit_Light завершён: сверенные с Figma карточки Popup,
  Bottomsheet, Floating Button, Sidebar 3.0, Skeletons, System (Spinner/Divider/
  MiniappButton), Mail, Tabs (+SegmentControl), Tag, TextArea, Userpic.
- Итого 30 компонентов описаны по Figma (оси вариантов и состояния — из имён
  вариантов), реестр INDEX полностью сверен (✔).

### Notes
- Пропущены компоненты со статусом `(In progress)`: Confirm, InputNumber (Stepper),
  FormsCard — по правилу отбора.

## [1.7.0] — 2026-08-11

### Added
- Сверенные с Figma карточки: Scroll Bar, Progress Bar, Pagination, Breadcrumbs,
  Dropdown (Legacy/V2, многоуровневое меню Levels), Header, Toolbar, SnackBar.

## [1.6.0] — 2026-08-11

### Added
- Сверенные с Figma карточки компонентов: Hint 2.0, Input, InputDate, InputSearch,
  MultiSelect, Select (оси вариантов и состояния — из имён вариантов в Figma).
- В реестре у них проставлены уровень и отметка сверки ✔.

## [1.5.0] — 2026-08-11

### Added
- Протокол ведения каталога `foundation/components/README.md`: атомарные слои
  (атомы/молекулы/организмы/шаблоны), правило «не выдумывать» (нет доступа → просить
  ссылку → описывать по странице-примеру), отметка сверки, уровень.
- Реальные карточки компонентов, сверенные с Figma: Cards, Cell (2.0), Controls,
  Empty Content / 404 (плюс уже бывший Action Button). У всех — уровень и отметка
  сверки в шапке.
- Реестр в `INDEX.md` с колонками «Уровень» и «Сверка».

### Changed
- `_TEMPLATE.md`: добавлены поля «Сверка» и «Уровень», ссылка на протокол.
- Бриф проекта: протокол описания компонентов + план каталога шаблонов
  (Life-04_Templates → `foundation/templates/`).

### Removed
- 30 черновых карточек, сгенерированных `/foundation` с выдуманными осями вариантов
  (node у всех был `5:2`, оси не из Figma). Заменяются сверенными по мере описания.

## [1.4.0] — 2026-08-11

### Added
- Реестр компонентов Kit_Light в `foundation/components/INDEX.md` со статусами
  страниц (✅ / ? / 🔍 / In progress).

### Changed
- Правило отбора: в каталог берём все компоненты, **кроме `(In progress)`**
  (статусы `?` и `🔍` тоже описываем, помечая, что решение может измениться).

## [1.3.0] — 2026-08-11

### Added
- `ux-lab/rules/07-voice-and-copy.md` — бренд-войс и UX-копирайт TN Life из двух
  документов (Tone of Voice + Редакционная политика): ценности, позиция на шкалах,
  правила текста, слова/обращение, заголовки, пунктуация, числа/дата/деньги, чек-лист.
- Каталог компонентов: `ux-lab/foundation/components/` — `INDEX.md`, `_TEMPLATE.md`
  и первая карточка `action-button.md` (описывает поведение и применение, без
  дублирования привязок токенов; статус In progress не берём).

### Changed
- Definition of Done (`rules/README.md`): раздел «Копирайт» расширен до
  «Копирайт и бренд-войс» со ссылкой на правило 07.

## [1.2.1] — 2026-08-11

### Changed
- Уточнены правила применения в `tokens.md`: цвета — только токены темы; палитра
  бренда — примитивы, напрямую не использовать; стили есть только у типографики;
  размеры/радиусы применяются токенами напрямую.

## [1.2.0] — 2026-08-11

### Added
- Реальные токены дизайн-системы «Life», выгруженные из Figma (W3C Design Tokens):
  - `ux-lab/foundation/tokens/` — исходный JSON (spaces, radiuses, brand,
    theme.light/dark, typography.web/mobile/legacy-proxima).
  - `ux-lab/foundation/tokens.md` — человекочитаемая сводка: отступы, радиусы,
    палитра бренда (7 семейств), семантические цвета Light/Dark со состояниями,
    типографика в трёх режимах (Web/Mobile/Legacy Proxima).
- Раздел «Как это применяется в Figma» в `tokens.md`: связка переменные → текстовые
  стили → режимы коллекции; правило «не хардкодить px».

### Changed
- `tokens.md` заменил placeholder-значения реальными.

## [1.1.0] — 2026-08-11

### Added
- `CLAUDE.md` — инструкция для CLI-агента: карта репо, правило «сначала загляни в
  память», протокол «фиксируй то, с чем долго возились» (проактивная запись
  паттерна), порядок версионирования. Claude Code читает её автоматически.
- `LICENSE` — MIT.
- Секция «Как пополнять и версионировать» + блок «Лицензия» в `README.md`.
- `.gitignore` — отсекает мусор macOS, `*.zip` и артефакты проекта-потребителя
  (`.claude/`, `brain/`, `PROJECT.md`, `CHANGELOG-DESIGN.md`).

### Changed
- Кит выделен в самостоятельный git-репозиторий (`main`, теги SemVer).

## [1.0.0] — 2026-08-11

### Added
- Первая версионированная сборка кита.
- `VERSION` и `CHANGELOG.md` — версионирование содержимого кита (SemVer).
- `RUNNING.md` — как запустить оркестр из CLI (macOS/Linux + Windows), с реальными
  командами ручной установки под Unix и порядком команд внутри Claude Code.
- Ядро `ux-lab/`: цикл hypothesis-driven design, плейбук (6 фаз), свод правил
  UI/UX (6 разделов + Definition of Done), токены из Figma, шаблоны гипотез,
  двухуровневая база лучших практик (`INDEX.md` + `patterns/`).
- `specs/`: шаблон и пример входной спеки для `/feature`.

### Changed
- `ux-lab/playbook/3-orchestrator.md` дополнен ручной установкой под macOS/Linux
  и ссылкой на `RUNNING.md`; первой командой сессии указан `/start`.
