# Designer's personal rules

Empty. The Chronicler fills this in after you approve - once the same edit
of yours repeats across projects.

Format: one rule, one bullet line. A rule worth handing to the whole team
(the kit's shared best-practices library) is marked at the end of THAT SAME line:

    - Show validation errors at the field (общий паттерн)

The marker applies only to its own line; on a heading it does nothing and does
not extend to neighbouring lines — that is deliberate. Marked lines are
candidates to send to Anton for the kit library (see `START-HERE.md`).